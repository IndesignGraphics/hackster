class Tool {
  String? toolId, category, title, image, quantity, price;
  int? availableStock;

  Tool({
    required this.toolId,
    required this.title,
    required this.image,
    required this.price,
    required this.quantity,
    required this.category,
    required this.availableStock,
  });

}
