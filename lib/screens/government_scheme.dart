import 'package:flutter/material.dart';

class GovernmentScheme extends StatelessWidget {
  const GovernmentScheme({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Government Scheme'),
    );
  }
}
