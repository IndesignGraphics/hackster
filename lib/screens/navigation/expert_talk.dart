import 'package:flutter/material.dart';

class ExpertTalk extends StatelessWidget {
  const ExpertTalk({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Expert Talk'),
      ),
      body: Center(
        child: Text('Expert Talk'),
      ),
    );
  }
}
