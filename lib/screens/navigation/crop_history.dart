import 'package:flutter/material.dart';

class CropHistory extends StatelessWidget {
  const CropHistory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Crop History'),
      ),
      body: Center(
        child: Text('Crop History'),
      ),
    );
  }
}
